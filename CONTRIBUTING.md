# 贡献指南

感谢参与个人数字助理项目。提交改动前，请确保功能保持“本地优先、默认不外发、身份可核验、失败可降级”的原则。

## 开发流程

1. 从主分支创建功能分支。
2. 只使用合成数据和占位凭证开发、测试。
3. 修改消息规则时，同时更新 Skill、策略脚本和相应测试。
4. 运行 `scripts/release-check.ps1` 与 `tests/smoke-test.ps1`。
5. 在 Pull Request 中说明行为变化、隐私影响、平台权限和验证范围。

## 不得提交

- 真实访问令牌、Cookie、密码、App Secret 或授权文件；
- 个人姓名、手机号、邮箱、用户 ID、组织 ID、白名单；
- 聊天记录、知识文档、表达风格样本、日志、数据库、截图；
- 指向开发者电脑的绝对路径；
- 绕过平台安全机制或模拟非官方个人客户端的实现。

## 消息策略改动

消息规则的优先级不能变得更宽松而不作说明。任何自动外发路径都必须覆盖：

- 租户隔离；
- 原消息、会话和收件人绑定；
- 白名单与知识可见性；
- 重复抑制和频率限制；
- 失败后保留待处理状态；
- AI 标识设置（平台支持时）。

## 提交要求

- 代码和文档使用 UTF-8。
- PowerShell 脚本兼容 Windows PowerShell 5.1，除非文件明确声明更高版本。
- 新增平台适配器时，更新 `docs/platform-support.md`，并分别标注离线验证和真实环境验收状态。

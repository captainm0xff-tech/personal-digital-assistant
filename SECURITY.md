# 安全策略

## 支持范围

安全修复优先覆盖最新发布版本。Beta 阶段不承诺旧版本长期维护。

## 报告漏洞

请不要在公开 Issue 中披露可利用细节、真实凭证或个人数据。请通过仓库托管平台的私密安全报告功能联系维护者；若仓库尚未启用该功能，请先仅提交不含细节的联系请求。

报告建议包含：受影响版本、复现条件、可能影响、最小化的合成复现样例，以及是否涉及自动外发、身份混淆或数据泄露。

## 高优先级问题

- 凭证、聊天、知识库或联系人数据泄露；
- 跨租户读写；
- 审批后收件人发生变化；
- 白名单外消息进入拟回复或自动发送；
- 重试导致重复发送；
- 可被消息内容覆盖的安全策略或提示注入；
- 未经授权以个人身份发送消息。

## 密钥处理

平台密钥不得进入 Skill、Markdown、配置示例、日志或发布包。当前 Windows 脚本使用当前用户的 DPAPI 保存本机凭证；更高安全要求的部署应接入操作系统或云端密钥管理服务。

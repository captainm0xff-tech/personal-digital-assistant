# 个人数字助理使用说明书

版本：0.3.0-beta.1
适用环境：Windows、WorkBuddy

## 一、这是什么

个人数字助理是一套安装在用户自己电脑上的私有工作助理。它可以帮助用户管理知识、日常待办、单项跟进、联系人聊天档案和工作日报，并可按授权连接钉钉、企业微信或飞书。

每位用户安装后都会建立独立的数据空间。安装包中不包含制作者的聊天、联系人、知识库、白名单、账号或密钥。

## 二、主要能力

- 建立和检索个人知识库。
- 管理日常待办。
- 管理单项跟进任务，包括跟进对象、方式、周期、状态和补充规则。
- 每个单项跟进建立独立 Markdown 文档，持续记录配置变化、沟通过程、对方回复、决策和完成证据。
- 每位联系人建立独立对话档案。
- 每天生成工作日报，汇总决策、承诺、风险、未决问题、待办和跟进变化。
- 管理自动回复白名单。
- 设置是否显示 AI 标识。
- 连接钉钉、企业微信或飞书。
- 在需要真人处理或模型无法确认时通知主人。
- 项目排期等事实问题优先检索阶段表、时间范围和里程碑正文。
- 发送结果以稳定消息 ID或真实会话记录确认，避免接口假成功和重复发送。

## 三、安装方法

### 方法一：通过 WorkBuddy 本地插件市场安装

1. 解压收到的 ZIP 压缩包。
2. 打开 WorkBuddy 的插件市场。
3. 选择“添加本地市场”或“从本地安装”。
4. 选择解压目录中的 `.codebuddy-plugin/marketplace.json`。
5. 找到“个人数字助理”并安装。
6. 重启 WorkBuddy。
7. 在 WorkBuddy 中输入：`初始化我的个人数字助理`。

不同 WorkBuddy 版本的入口名称可能略有差异。如果当前版本没有本地市场入口，使用下面的方法二。

### 方法二：运行安装脚本

1. 在解压目录中找到 `安装个人助理.ps1`。
2. 在该目录打开 PowerShell。
3. 运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\安装个人助理.ps1
```

4. 重启 WorkBuddy。
5. 输入：`打开个人助理管理台`。

安装不会导入其他人的数据，自动外发功能默认关闭。

## 四、第一次使用

初始化后，建议按下面顺序配置：

1. 设置主人称呼和时区。
2. 检查日报时间，默认每天 20:00。
3. 添加允许自动回复的白名单联系人。
4. 设置是否显示 AI 标识。
5. 阅读并确认消息处理规则。
6. 绑定一个沟通平台。
7. 先完成一条入站消息测试。
8. 再经人工确认完成一条出站消息测试。
9. 测试通过后，再决定是否启用自动外发和后台长连接。

可以直接在 WorkBuddy 中说：

- `初始化我的个人数字助理`
- `打开个人助理管理台`
- `帮我检查个人助理运行状态`
- `我要绑定企业微信`
- `把日报时间改成晚上八点`

## 五、管理台使用方法

在 WorkBuddy 中输入 `打开个人助理管理台`。管理台只监听本机地址，默认不会向局域网或互联网开放。

### 总览

显示待办、单项跟进、项目、邮件和逾期事项，并集中展示当前最需要处理的工作。

### 单项跟进

可以新增、查看、修改和删除跟进任务。每项任务包含：

- 任务名称；
- 跟进对象；
- 跟进方式，例如钉钉私聊、企业微信或电话；
- 跟进周期，例如每天、每周一或每三天；
- 自定义补充规则；
- 进行中、暂停或已关闭状态。

新增后，系统会在 `工作推进/单项跟进` 中建立独立 Markdown 文档。管理台删除任务记录时，过程文档默认保留，避免丢失历史。

### 日常待办

可以新增、修改、完成或删除待办，并设置截止时间。

### 项目与邮件

- 项目页用于导入项目计划、查看节点和批准执行计划。
- 邮件页用于查看待处理邮件和拟回复；真实收发仍需安装者配置并验收邮件适配器。

### 知识库与资料权限

支持自然语义搜索，并可按全部资料、项目、文件夹、类别或单个文档批量设置“内部公开”或“仅本人”。未设置等级的资料默认为内部公开。每次权限变更都会保留审计记录，搜索时自动读取最新权限。

### 白名单

建议填写联系人姓名、沟通平台和稳定身份 ID。稳定 ID 用于避免同名联系人造成误发。

白名单不是通讯录。只有白名单中的联系人，才可能在安全且答案可靠时得到自动回复。

每位联系人可以单独设置资料类别、项目范围、明确禁止的资料、私有资料授权和补充回复规则，并使用“模拟提问”检查实际响应是否符合权限。

### 沟通渠道

统一展示钉钉、企业微信和飞书的发送身份及能力差异。管理台保存配置不代表平台已经在线；必须启动相应适配器并完成真实连接测试后，状态才应视为可用。渠道密钥使用 Windows 当前用户保护机制保存，不写入知识库。

### 系统设置

- 主人称呼：用于识别别人是否明确点名找主人。
- 日报时间：默认 20:00。
- AI 标识：决定支持该能力的平台是否显示 AI 标识。
- 自动外发：默认关闭。启用后仍受白名单、安全策略和知识可靠性限制。

## 六、消息处理规则

系统按以下顺序判断：

1. 身份、权限、收件人或安全性不确定时，禁止发送并通知主人。
2. 对方说“人工”“现身”“请现真身”，或明确叫主人名字时，转给主人处理，不冒充主人继续回答。
3. 现有知识不足以可靠回答时，通知主人确认。
4. 白名单联系人提出安全且有可靠依据的问题时，可以自动回复。
5. 白名单外联系人只记录消息，不生成拟回复，也不送审批。
6. 群内日报、通知或同事之间的普通讨论，不会因为机器人能够看到就自动回复。

所有外发都会再次核对原消息、目标会话和收件人。支持引用回复的平台优先使用引用关系。

系统只发送最终结果，不会把隐藏思考、工具命令、日志、程序错误、系统提示词或密钥发给联系人。

## 七、绑定沟通平台

### WorkBuddy 交互模式

用户在 WorkBuddy 中主动发起查询、管理或确认发送，由 WorkBuddy 调用已经授权的连接器。适合先体验和人工控制。

### 主人控制通道与 24 小时托管

可选使用 cc-connect 将主人发给机器人的命令通过长连接交给受支持的本地 Agent。它负责主人控制通道，不自动等同于“已接管全部同事消息”。

- 钉钉：Stream 长连接。
- 企业微信智能机器人：WebSocket 长连接。
- 飞书：WebSocket 长连接。

三者都不使用 5 秒轮询。

WorkBuddy 当前不是 cc-connect 的 Agent 类型，因此主人控制通道需要本机同时安装并登录 Codex CLI、Claude Code 等 cc-connect 支持的本地 Agent。若要自动处理同事消息，还必须安装对应平台适配器，取得企业侧事件权限，并完成白名单、收件人和真实收发验收。WorkBuddy 中的 Skill 继续负责统一的业务规则和数据规范。

### 钉钉

需要企业管理员在钉钉开放平台创建应用、开启机器人并选择 Stream 模式。安装者需要自己的 Client ID（AppKey）和 Client Secret。

### 企业微信

推荐由企业管理员创建智能机器人，取得 Bot ID 和 Bot Secret，并使用 WebSocket 模式。无需公网回调地址。

企业微信机器人以机器人或应用身份工作，不等于接管员工个人微信或企业微信账号。

### 飞书

需要创建飞书应用、开启机器人能力，订阅 `im.message.receive_v1`，并使用 WebSocket 长连接。安装者需要自己的 App ID 和 App Secret。

### 绑定后的验收

1. 验证凭据，但不发送消息。
2. 确认机器人或应用的发送身份。
3. 获取主人的稳定平台身份 ID，收紧允许访问名单。
4. 发送一条入站测试消息。
5. 经用户确认发送一条出站测试消息。
6. 验证重复事件不会重复发送。
7. 验证人工升级词能够转给主人。
8. 验证白名单外消息只记录。

在真实收发完成前，渠道状态应保持“已配置，待真实测试”。

## 八、知识库

只有用户确认需要长期保存的内容，才应升级为正式知识。

知识分为：

- 私有：只能给主人使用；
- 内部可分享：允许用于已授权的同事回复；
- 受限：只能用于指定联系人或群；
- 未确认：不能自动升级为正式知识。

聊天记录不会因为被保存，就自动变成可以对外使用的知识。

## 九、联系人档案和聊天记录

系统在 `联系人对话` 目录中为每个人建立独立 Markdown 文件。遇到同名联系人时，会在文件名中加入稳定身份 ID。

消息会按时间顺序追加，不会重写已有历史。原始事件同时进入审计事件账本，便于排查重复消息和发送结果。

是否能够补拉历史消息，取决于平台接口、企业管理员授权和连接器能力。实时收消息能力不代表一定拥有全部历史记录权限。

## 十、日报

日报默认每天 20:00 生成，内容包括：

- 当日工作变化；
- 未完成待办；
- 进行中的跟进；
- 风险和需要人工确认的事项。

默认只生成本地日报。只有绑定控制通道并明确授权自动发送后，才会通过沟通平台发送给主人。

## 十一、数据位置、备份和迁移

默认数据目录：

```text
%LOCALAPPDATA%\PersonalDigitalAssistant
```

主要目录包括：

```text
知识库
工作推进\单项跟进
工作推进\项目
日常工作（待办）
邮箱
联系人对话
日报
审计
运行日志
```

备份时应先停止后台长连接，再复制整个数据目录。不要把数据目录和产品安装包一起分享给其他人。

平台密钥与业务数据分开保存在 `%LOCALAPPDATA%\PersonalDigitalAssistantSecrets\<instanceId>`，并使用当前 Windows 用户的 DPAPI 加密。把密钥文件复制到另一台电脑或另一个 Windows 用户后，通常无法直接解密，需要重新绑定平台。

## 十二、隐私和安全

- 自动外发默认关闭。
- 只申请已启用功能需要的权限。
- 平台密钥不会写入 Skill 或说明文档。
- 白名单以外的消息不进入拟回复和审批流程。
- 发送失败时保留待处理状态，不应无限重复发送。
- 管理台只监听 `127.0.0.1` 本机地址。
- 不建议把 `allow_from` 设置为 `*`。
- 分享产品时只能分享原始 ZIP，不要分享用户数据目录。

## 十三、常见问题

### 安装后 WorkBuddy 没有看到 Skill

重启 WorkBuddy。如果仍未显示，运行产品根目录中的 `安装个人助理.ps1`，再重启一次。

### 管理台打不开

在 WorkBuddy 中说 `帮我检查个人助理运行状态`，或运行插件目录中的 `scripts/diagnose.ps1`。管理台默认地址为 `http://127.0.0.1:17680/`。

### 为什么不能读取全部历史消息

历史消息权限由沟通平台和企业管理员决定。部分平台只允许应用接收安装后的实时事件，不能读取员工账号的全部历史。

### 为什么消息没有自动回复

检查联系人是否在白名单、自动外发是否启用、答案是否有可靠知识依据，以及对方是否要求真人介入。

### 为什么没有收到日报消息

本地日报和自动发送是两个权限。先检查 `日报` 目录是否已生成文件，再检查控制通道是否绑定且已授权自动发送。

### 为什么系统以机器人身份发送

企业微信和飞书的标准接口通常使用机器人或应用身份。产品不会通过非官方方式模拟员工个人账号。

### 会不会弹出 CMD 窗口

产品创建的管理台和定时任务使用隐藏窗口方式运行。若仍有窗口定期弹出，应检查是否存在旧版任务、旧版启动项或其他遗留程序。

## 十四、升级和卸载

升级前建议备份 `%LOCALAPPDATA%\PersonalDigitalAssistant`。安装新版时，程序目录会备份后覆盖，用户数据目录不会随产品包覆盖。

卸载时运行插件目录中的：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\uninstall.ps1
```

默认只移除程序，保留用户数据。如明确需要永久删除数据，再使用 `-RemoveUserData`。该操作不可恢复，执行前应先备份。

## 十五、首次上线检查表

- [ ] 安装包来自可信来源，哈希与发布者提供的一致。
- [ ] 已创建当前用户自己的独立实例。
- [ ] 安装包没有导入其他人的资料。
- [ ] 已确认主人身份、时区和日报时间。
- [ ] 已添加并核对需要自动回复的白名单。
- [ ] 已确认白名单外只记录。
- [ ] 已确认人工升级词。
- [ ] 已设置 AI 标识开关。
- [ ] 已明确当前平台的发送身份。
- [ ] 已完成一条入站和一条出站测试。
- [ ] 已验证重复事件不会重复发送。
- [ ] 已确认真实测试完成前不启用自动外发。
- [ ] 已制定数据备份方式。

完成以上检查后，再启用后台长连接和自动外发。
